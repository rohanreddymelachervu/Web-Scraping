The Crawler.py file is a Python Web Crawler using BeautifulSoup. It crawls web pages within the main link, which is read from a txt file (seed1.txt)
It then creates a new directory in which it stores the crawled content.
